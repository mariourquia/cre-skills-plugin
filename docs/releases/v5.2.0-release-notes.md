---
title: v5.2.0 Release Notes
version: 5.2.0
date: 2026-06-04
status: released
---

# v5.2.0 — Skill-contract + governance-metadata foundation

## Summary

v5.2.0 completes the v5 skill-contract and governance-metadata foundation. It does
**not** grow the catalog (still 127 skills / 54 agents / 21 calculators / 10
orchestrators / 6 workflow chains — **0 net new skills**); the value is a
corpus-wide governance contract, consumer-ready governance metadata, and a
corpus-wide **static** governance scanner. It is explicit about what is validated
statically versus what remains runtime work.

This release introduces **no live connector, no production runtime enforcement,
and no live external coupling**. Every connector contract and adapter stays
`status: stub`, the skill-manifest export stays a static export (`live_connector:
false` on every entry), and the generalized runtime emitted-output scanner remains
deferred (see Known Limitations).

## What shipped (each landed with tests)

### Full-corpus skill contract (tiered)
- **Tier 1 (all 127 skills):** every skill now carries valid, non-null
  `classification` and `runtime_role` plus the v5.2 forward-compat fields, all
  enum-validated corpus-wide (`tests/test_skill_contract_corpus.py`). This is the
  "all 127 contract-checked" floor — derived by `catalog-build.py` with frontmatter
  override, so it is metadata + validation, **not** stub-skill inflation.
- **Tier 2 (decision-grade set):** the nine decision-grade / `final_marked`
  carriers are brought to full `v5_contract: true` conformance — `## Refusal
  Behavior`, `## Confidence and Provenance`, `## Known Limitations`, and the
  `stale_data` / `confidence_default` / `refusal_trigger` fields. Orchestrators and
  workspaces, which delegate every number to specialist skills, carry the
  Known-Limitations-only contract.

### Consumer-ready governance metadata (manifest forward-compatibility)
The skill manifest export now carries **meaningfully populated** forward-compat
fields (manifest contract bumped to `1.1`):
- **`produces_artifact_kind`** — a stable, plugin-namespaced artifact vocabulary
  (`memo`, `model_output`, `calculator_result`, `diligence_report`, `source_map`,
  `tie_out_report`, `investor_report`, `lender_package`, `valuation_support`,
  `checklist`, `workflow_plan`, `advisory_note`). Derived (calculator-backed →
  `calculator_result`, orchestrator → `workflow_plan`) with explicit frontmatter
  override on the decision-grade / consumer-facing set.
- **`pii_policy`** — refined into a sensitivity ladder: `none` → `business_contact`
  → `tenant_or_personal` → `sensitive_financial`. Conservative default `none`
  (explicit opt-in, never over-claimed); a non-`none` value must be an explicit
  frontmatter declaration. **PHI is intentionally not a value — CRE Skills do not
  accept PHI.** (The never-populated v5.1 posture enum is replaced; no previously
  emitted value changes meaning.)
- **`workspace_scope`** — widened to the full CRE workspace set (`asset`, `fund`,
  `portfolio`, `deal`, `debt`, `leasing`, `property_management`,
  `investor_relations`, `governance`, `market`, `data_room`, `enterprise`) and
  derived from each skill's slug/subcategory with frontmatter override.
- **`outputs[]`** — backfilled with named output artifacts (symmetric with
  `input_artifacts`) on artifact-producing and consumer-facing skills.

### Corpus-wide static governance scanner
- **`scripts/governance-scan.py`** — a corpus-wide **static + generated-artifact**
  scanner. It validates governance *declarations* over SKILL.md frontmatter (read
  from the filesystem so a stale catalog cannot mask a removed disclaimer) and the
  generated catalog/manifest. Fail-closed (ERROR) rules: decision-grade ⇒ human
  gate + source-ref policy + refusal trigger; source-ref refusal posture;
  valuation output ⇒ "not an appraisal" stamp; statute-encoding (tax/legal) skill ⇒
  fresh `statute_review` + "not legal/tax advice" disclaimer; calculator-backed ⇒
  resolvable `calculator_result`; decomposition references resolve; PII never
  over-claims. WARN rules surface completeness/backfill candidates. Liability rules
  trigger on **explicit** governance fields, never description keyword-sniffing, so
  generalizing them cannot conscript advisory skills into stub inflation.
- Data-driven fixtures (`tests/test_governance_scan.py`) prove each rule fails
  closed, with a positive control on the real catalog. Wired into CI.
- **This is static declaration validation, not runtime enforcement.** See below.

### Connector / source-class no-live invariants
- Top-level, CI-runnable invariants (`tests/test_connector_stub_invariants.py`)
  pin the no-live guarantee: every connector domain and adapter manifest stays
  `status: stub`, no entity declares a live `source_class`, and the `source_class`
  enum matches its canonical list. `source_class` schema work from v5.1.0 is
  unchanged (no regression).

## What is still deferred (honest)

- **Generalized runtime emitted-output governance scanner.** Tagging/refusing every
  cell of every decision-grade skill *at emit time* is deployed **only inside the
  `residential_multifamily` subsystem**. The corpus-wide scanner shipped here
  validates **declarations**, not emitted output. Generalizing the runtime machinery
  to the whole corpus remains future work (see `docs/ROADMAP.md`).
- **Live connectors / runnable adapters.** All connector contracts and adapters are
  `status: stub`; `source_class` / `max_staleness` are declared and schema-validated,
  not enforced at a connector runtime.
- **PII/confidentiality backfill is meaningful, not exhaustive.** Clear personal-
  data-adjacent skills declare a sensitivity tier; the static scanner WARNs on the
  remaining candidates rather than silently defaulting them.
- **`statute_review` freshness is a scheduled CI liability.** The tax/legal
  statute-verification dates re-verify on a 24-month horizon (next trip 2028-06-03);
  re-verification is a maintenance task, not a one-time fix.

## Counts

Unchanged from v5.1.0: **127 skills · 54 agents · 21 calculators/MCP tools · 10
orchestrators · 6 workflow chains · 0 net new skills.**

## Verification at authoring time

`pytest tests/` passes (479+ tests; the new corpus-contract, governance-scanner,
and connector-stub-invariant suites are green and the extended manifest-contract,
skill-contract, and classification suites pass); `governance-scan` reports 0
errors / 0 warnings on the corpus;
`version_check`, `registry-validator`, `catalog-generate --check`, and
`validate-marketplace.sh` pass at 5.2.0.
