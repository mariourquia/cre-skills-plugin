# Session Workspace

This directory stores local session state and rendered preview artifacts for live implementation-intake engagements.

- Expected live files:
  - `{engagement_id}/{session_id}.yaml`
  - `{engagement_id}/{session_id}__preview.md`
  - `{engagement_id}/{session_id}__leader_signoff.md`
  - `{engagement_id}/{session_id}__action_queue.md`
- The directory is ignored by git on purpose.
- Committed examples live under `../examples/`, not here.
