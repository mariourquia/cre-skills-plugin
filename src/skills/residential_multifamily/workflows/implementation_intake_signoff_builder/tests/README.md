# Workflow Test Notes

Implementation-intake validation is executed from the subsystem pytest suite under:

- `src/skills/residential_multifamily/tests/test_implementation_intake_signoff_builder.py`

This local `tests/` directory exists to keep the workflow package self-describing and to reserve space for future workflow-specific unit tests or fixtures if the session engine grows more complex.
