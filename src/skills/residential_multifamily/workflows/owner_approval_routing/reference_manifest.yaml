pack_slug: owner_approval_routing
reads:
  - path: reference/normalized/approval_threshold_defaults.csv
    category: approval_threshold_policy
    expected_schema_version: "0.1.0"
    required: true
    fallback_behavior: refuse
  - path: reference/normalized/approval_routing_policy__{org}.yaml
    category: approval_threshold_policy
    expected_schema_version: "0.1.0"
    required: true
    fallback_behavior: refuse
writes:
  - path: reference/derived/approval_audit_log.jsonl
    category: approval_threshold_policy
    write_mode: append
