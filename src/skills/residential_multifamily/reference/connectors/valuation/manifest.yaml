# Valuation connector manifest (vendor-neutral v5.1 stub).
# Conforms to reference/connectors/_schema/connector_manifest.schema.yaml.
connector_id: valuation
connector_kind: valuation
version: 0.1.0
status: stub
owner: residential_multifamily_subsystem
vendor_neutral: true
description: |
  Valuation feed. Carries property valuation events (appraisals, internal
  marks) with the concluded value, approach, and as-of date.
authoring_notes: |
  v5.1 stub contract. Schema only; no live adapter exists and nothing
  executes. A future adapter (appraisal vendor / valuation platform export)
  replaces the file_drop handshake with the source system's native auth.
entities:
  - valuation
provenance_fields_required:
  - source_name
  - source_type
  - source_date
  - source_row_id
handshake:
  auth_kind: file_drop
  access_pattern: pull_scheduled
  notes: |
    Stub. A future adapter replaces file_drop with the valuation source
    system's native auth kind and access pattern.
contact: operator_placeholder
